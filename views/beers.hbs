{{! -- What we need to do:
each.data and then for every element show me name of beer and
 image of beer b/c each object is one of the beers.
in terminal you see how all elements are structured. --}}

<ul>
    {{#each data}}
    {{!-- @index, @key, @first (first iteration of loop), @last--}}
 {{!--<img src="{{this.image_url}}" /> --}}
 
 {{> beerpartials this}}

    {{/each}}
</ul>